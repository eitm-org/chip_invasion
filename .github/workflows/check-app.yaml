# Workflow derived from https://github.com/rstudio/shinytest2/tree/main/actions/test-app/example-test-app-description.yaml
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches:
      - main
      - elizabeth
      - xchen_descr_method 

name: Test app w/ DESCRIPTION

jobs:
  test-app:
    runs-on: ubuntu-latest

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: ubuntu-latest, r: 4.2.2}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v2

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true
<<<<<<< HEAD
=======
          
      - uses: r-lib/actions/setup-renv@v2
>>>>>>> a9f18feaf2a5f3e2d706769eb299642f216b7274

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
<<<<<<< HEAD
          extra-packages:
            shinytest2
      
      - name: Test app (main)
        uses: rstudio/shinytest2/actions/test-app@v1
        with:
=======
>>>>>>> a9f18feaf2a5f3e2d706769eb299642f216b7274
          app-dir: "chip_invasion"
          upload-snapshots: true
